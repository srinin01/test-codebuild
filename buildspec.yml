version: 0.2

phases:
  install:
    commands:
    - curl -s -qL -o terraform_install.zip https://releases.hashicorp.com/terraform/1.0.11/terraform_1.0.11_linux_amd64.zip
    - unzip terraform_install.zip -d /usr/bin/
    - chmod +x /usr/bin/terraform
    finally:
      - terraform --version
  build:
    commands:
      - |
        enter="\n"
        tinit=$(terraform init)
        tplan=$(terraform plan)
        output="#### Terraform Initialization 🖌 $enter$tinit"
        echo $output
        curl \
          -X PATCH \
          -H "Accept: application/vnd.github.v3+json" \
          -H "Authorization: Bearer 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" \
          https://api.github.com/repos/srinin01/test30/pulls/2 \
          -d '{ "title": "srini" }'
      - echo build done!
